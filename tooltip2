
<Tooltip title={params.value}>
  <Typography variant="body1" style={{ fontSize: '14px' }}>
    {params.value}
  </Typography>
</Tooltip>
div style={{ overflow: 'hidden', whiteSpace: 'nowrap', textOverflow: 'ellipsis' }}>
    {params.value}
  </div>

renderCell: (params) => (
      <div style={{ whiteSpace: 'pre-wrap', overflowWrap: 'break-word', wordWrap: 'break-word' }}>
        {params.value}
      </div>
    )
=====================


<Tooltip
  title={params.value}
  sx={{
    fontSize: '14px' // Change the font size as needed
  }}
>
  <div style={{ overflow: 'hidden', whiteSpace: 'nowrap', textOverflow: 'ellipsis' }}>
    {params.value}
  </div>
</Tooltip>
